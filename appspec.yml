version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/html/my-temp-dir
    overwrite: true
    fileExistsBehavior: OVERWRITE
permissions:
  - object: /var/www/html/my-temp-dir
    owner: ubuntu
    group: ubuntu

hooks:
  BeforeInstall:
    - location: scripts/set_destination.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: scripts/integrations-deploy-post.sh
      runas: root


