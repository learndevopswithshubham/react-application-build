version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/html/
    overwrite: true
    fileExistsBehavior: OVERWRITE

hooks:
  BeforeInstall:
    - location: scripts/set_destination.sh
      timeout: 300
      runas: root


